/** Create a tournament with the given format, entry fee, base wager, and max players */
export declare function createTournamentCommand(format: string, maxPlayers: string, opts: any): Promise<void>;
